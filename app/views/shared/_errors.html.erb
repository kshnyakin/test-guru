<% if resource.errors.any? %>
  <div>
    <h2>
      <%= pliralize(resource.errors.count, 'error') %>:
    </h2>

    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
