<tr>
  <td><%= question.title %></td>
  <td><%= question.body %></td>
  <td><%= link_to 'show', test_question_path(question) %></td>
  <td><%= link_to 'edit', edit_test_question_path(question) %></td>
  <td>
    <%= link_to 'Delete',
      test_question_path(question),
      method: :delete, 
      data: {
        confirm: "Вы действительно хотите удалить вопрос '#{question.title}' ?"
      }
    %>
  </td>
</tr>
